let Person;_b91‍.x([["Car",()=>Car],["Truct",()=>Truct]]);_b91‍.w("./Person",[["default",["Person"],function(v){Person=v}]]);

class Car{
    constructor(brand, model)
    {
        this.brand = brand
        this.model = model
        
    }

    set driver(driver){
        if(driver instanceof Person){
            let msg = "Person set as a driver"
            msg += "\nname:"+driver.firstName+" "+driver.lastName
            msg += "\nsalary:"+driver.salary
            _b91‍.g.console.log(msg)
            this._driver = driver
        }
        else{
            console.log("Error setting driver")
        }
    }

    get driver(){
        return this._driver
    }
}

class Truct extends Car {
    constructor(brand,model,hv){
        super(brand,model)
        this.hv = hv
    }
}

